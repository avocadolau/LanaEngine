#include "lnpch.h"

struct aiMesh;

typedef char sbyte;
void Test();

namespace Lanna {

	/*class MeshImporter {

	
	private:
		unsigned int GetSize(const aiMesh* mesh);
		void WriteBuffer(const aiMesh* mesh, char* cursor);
	public:

		std::string ImportMesh(const char* file);
	};*/
	
	

	
}